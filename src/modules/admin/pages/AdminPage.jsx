import { AdminLayout } from '../layout/AdminLayout';
import { Default } from '../views/Default';

export const AdminPage = () => {
  return (
    <AdminLayout>
      <Default />
    </AdminLayout>
  );
};
